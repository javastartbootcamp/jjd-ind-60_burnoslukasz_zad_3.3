public class Operation {
    boolean isEven(int x) {
        return x % 2 == 0;
    }

    boolean isOdd(int x) {
        return x % 2 != 0;
    }

    double circleField(double rad) {
        return 3.14 * (rad * rad);
    }

    int power(int x) {
        return x * x;
    }
}
